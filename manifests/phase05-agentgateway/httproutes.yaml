apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: account-mcp-route
  namespace: trustusbank-platform
spec:
  parentRefs:
    - name: trustusbank-agentgw
  rules:
    - matches:
        - path: { type: PathPrefix, value: /mcp/account }
      backendRefs:
        - group: agentgateway.dev
          kind: AgentgatewayBackend
          name: account-mcp
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: transaction-mcp-route
  namespace: trustusbank-platform
spec:
  parentRefs:
    - name: trustusbank-agentgw
  rules:
    - matches:
        - path: { type: PathPrefix, value: /mcp/transaction }
      backendRefs:
        - group: agentgateway.dev
          kind: AgentgatewayBackend
          name: transaction-mcp
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ticket-mcp-route
  namespace: trustusbank-platform
spec:
  parentRefs:
    - name: trustusbank-agentgw
  rules:
    - matches:
        - path: { type: PathPrefix, value: /mcp/ticket }
      backendRefs:
        - group: agentgateway.dev
          kind: AgentgatewayBackend
          name: ticket-mcp
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: evil-tools-route
  namespace: trustusbank-platform
spec:
  parentRefs:
    - name: trustusbank-agentgw
  rules:
    - matches:
        - path: { type: PathPrefix, value: /mcp/evil }
      backendRefs:
        - group: agentgateway.dev
          kind: AgentgatewayBackend
          name: evil-tools
